meta {
  name: openapi
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/docs/json
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("is an OpenAPI document", function() {
    expect(res.body.openapi || res.body.swagger).to.be.a("string");
  });
}
